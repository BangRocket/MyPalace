# v0.12.0 schema-flip — deferred follow-ups

Two items were split out of the v0.12.0 work (see
`2026-05-31-v0.12.0-schema-flip.md`) by decision on 2026-05-31. The
v0.12.0 production code is complete and unit-verified (632 tests green);
these two are about **integration-test verification**, which is not run
in CI today.

---

## Follow-up 1 — Fix the pre-existing Alembic stamp bug — ✅ DONE

Resolved in commit `fix(alembic): widen version_num column + correct
LATEST_ALEMBIC_REVISION (#12)`. `_ensure_alembic_stamp` + `alembic/env.py`
now create `version_num VARCHAR(255)` (and widen an existing narrow
column); `LATEST_ALEMBIC_REVISION` points at the real head; a guard test
(`tests/test_alembic_revision_guard.py`) keeps it in sync; and
`test_alembic_live.py` invokes alembic via `python -m alembic` with the
correct repo cwd. Integration suite went from **50 setup errors → 45
passing**. Original write-up kept below for context.

---

## Follow-up 1 (original write-up) — Fix the pre-existing Alembic stamp bug (not schema-flip related)

**Symptom:** the integration suite errors in fixture setup, all tests:

```
INSERT INTO alembic_version (version_num) VALUES ('2026_05_05_0010_per_tenant_shadow_copy')
→ asyncpg StringDataRightTruncationError: value too long for type character varying(32)
```

**Root cause (pre-existing, unrelated to v0.12.0):**
- `mypalace/database.py` `_ensure_alembic_stamp()` hardcodes
  `alembic_version.version_num VARCHAR(32)`, but the project's revision
  ids exceed 32 chars (`2026_05_05_0010_per_tenant_shadow_copy` = 38,
  `..0011_emotional_contexts` = 34, `..0013_backfill_tenant_id` = 34).
- `LATEST_ALEMBIC_REVISION` in `mypalace/database.py:69` is **stale** —
  pinned at `2026_05_05_0010_per_tenant_shadow_copy` while the migration
  head is now `2026_05_31_0013_backfill_tenant_id`. On a fresh DB,
  `init_db()` create_all's every table then stamps `0010`; a subsequent
  `alembic upgrade head` would then try to re-run `0011`/`0012`'s
  `op.create_table` against tables that already exist.

**Why CI didn't catch it:** `.github/workflows/release.yml` runs only
`pytest tests/` (unit) and `pytest mypalace_client/tests/`. The
integration suite (`-m integration`) is never run in CI.

**Proposed fix:**
1. Widen the stamp column: in `_ensure_alembic_stamp`, create
   `version_num VARCHAR(64)` (and, if a real `alembic upgrade` ever
   creates the table, ensure `alembic/env.py` /
   `context.configure(version_table_...)` agrees — confirm what width
   real Alembic uses for this project, since 0.11.x shipped 34/38-char
   ids).
2. Update `LATEST_ALEMBIC_REVISION` to the actual head
   (`2026_05_31_0013_backfill_tenant_id`) so fresh-deploy stamping +
   `alembic upgrade head` are a no-op, not a conflict.
3. Add a unit/lint guard that `LATEST_ALEMBIC_REVISION` equals the single
   Alembic head (catches future staleness).

---

## Follow-up 2 — Rework integration fixtures for mandatory schema mode — ✅ DONE

Resolved in commit `test(integration): make schema-mode integration suite
fully green (#13)`. Group A tests now scope their direct `async_session`
access with `tenant_scope("test")` + `tenant_id="test"` (matching the
auth-disabled HTTP path); Group B alembic tests use a new `fresh_db_url`
fixture (throwaway database) to avoid the shared-session-DB collision;
and `_truncate_tables` also cleans the `test` schema. **Integration suite:
50 passed.** Original write-up kept below for context.

---

## Follow-up 2 (original write-up) — Rework integration fixtures for mandatory schema mode

The integration suite was written for table mode and needs adapting now
that schema isolation is always on. Do this with live containers (Docker
available) and iterate.

**Findings (from `tests/integration/conftest.py`):**
- The suite runs **auth-disabled** (`PALACE_AUTH_DISABLED=true`,
  `PALACE_DEFAULT_TENANT_ID=test` from `tests/conftest.py`). There is **no
  `admin_headers` / key-minting fixture** (the original plan's T8 wrongly
  assumed one). Tests target tenants via direct service calls or the
  `tenant_id` query param.
- With auth disabled, `AuthMiddleware` seats `current_tenant="test"` for
  every HTTP request → HTTP-path writes now land in the `test.*` schema.
  But direct service calls in test bodies set no contextvar → they hit
  `public.*` (which Approach A keeps + filters, so they still work).
- `_truncate_tables` (autouse) deletes from `public.*` with no tenant
  scope → it will **not** clean `test.*` data written via HTTP paths,
  risking cross-test leakage.
- `_ensure_default_tenant()` now (v0.12.0, T3) replicates the `test`
  schema automatically, so the test schema exists — no extra provisioning
  needed in the fixture.

**Concrete failing tests (after Follow-up 1, full suite = 45 passed / 5
failed).** These 5 are the work-list — all test-infra, not production
bugs (45 passing tests, incl. `test_tenancy_live`, exercise the
per-tenant schema paths via HTTP and pass):

*Group A — direct-write-then-HTTP (the `public.*` vs `test.*` split):*
- `test_dynamics_live.py::test_demote_records_failure_log_live`
- `test_dynamics_live.py::test_prune_access_logs_live`
- `test_intentions_live.py::test_cleanup_expired_intentions_live`

  Each seeds rows via `async_session()` **without** `tenant_scope` (→
  contextvar None → writes to `public.*`), then calls an HTTP endpoint
  (auth-disabled → contextvar `test` → operates on `test.*`). The HTTP
  side sees zero rows. **Fix:** wrap the test's direct seed writes in
  `with tenant_scope("test"):`, or add an autouse fixture that seats the
  contextvar to `test` for the whole test so direct + HTTP agree.

*Group B — pristine-DB alembic tests vs the autouse init fixture:*
- `test_alembic_live.py::test_alembic_upgrade_head_creates_full_schema`
- `test_alembic_live.py::test_alembic_downgrade_then_upgrade_is_clean`

  The autouse `_truncate_tables` fixture depends on `palace_app` →
  `init_db()` → `create_all`, which populates the **shared session DB**
  before every test. These two tests then run `alembic upgrade head`
  against an already-populated `public` schema → `CREATE TABLE … already
  exists`. **Fix:** give these a function-scoped fresh Postgres (or
  `DROP SCHEMA public CASCADE; CREATE SCHEMA public` at test start), and
  exempt them from the autouse init.

**Also add** the isolation + cross-tenant-aggregation tests originally
drafted for T8, adapted to the auth-disabled model (create tenants via
`POST /v1/admin/tenants`; drive per-tenant writes/reads via the
`tenant_id` param / cross-tenant admin behavior):
- tenant A's memory is invisible to tenant B (search_path isolation),
- `stats?tenant_id=ALL` reports each tenant's own counts,
- export/import round-trips into the right schema.

---

## Verification status at hand-off

- Unit suite: **632 passing**, ruff clean, `tenant_schema_mode` grep empty,
  single Alembic head.
- Integration suite: **blocked** by Follow-up 1 (pre-existing). Not yet
  exercised against the v0.12.0 changes.
